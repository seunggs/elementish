import React from 'react'
import R from 'ramda'

const Dashboard = React.createClass({
  componentDidMount() {
  },
  render() {
    return (
      <div>Dashboard</div>
    )
  }
})

export default Dashboard
